//
//  HomeView.swift
//  Kindergarten
//

import UIKit
/*
enum kHomeTexts: String {
    case eCellId = "HomeItemCell"
}

final class HomeView: UIView {
//    private let log = Log()
    let mainCategoryTableView = UITableView(frame: .zero, style: .grouped)
    let cellId = kHomeTexts.eCellId.rawValue
    
    init() {
        super.init(frame: UIScreen.main.bounds)
        backgroundColor = UIColor.white
        addTableView()
    }

    required init?(coder: NSCoder) {
        fatalError("HomeView init(coder:) has not been implemented")
    }
    
    // MARK: - Add Controls
    
    private func addTableView() {
        mainCategoryTableView.translatesAutoresizingMaskIntoConstraints = false
        mainCategoryTableView.sectionFooterHeight = 0.0
        mainCategoryTableView.register(UITableViewCell.self, forCellReuseIdentifier: cellId)
        mainCategoryTableView.alwaysBounceVertical = true
        addSubview(mainCategoryTableView)
        addTableViewConstraints()
    }
    
    // MARK: - Add Contsraints
    
    private func addTableViewConstraints() {
        NSLayoutConstraint.activate([
            mainCategoryTableView.topAnchor.constraint(equalTo: safeAreaLayoutGuide.topAnchor),
            mainCategoryTableView.leadingAnchor.constraint(equalTo: safeAreaLayoutGuide.leadingAnchor),
            mainCategoryTableView.trailingAnchor.constraint(equalTo: safeAreaLayoutGuide.trailingAnchor),
            mainCategoryTableView.bottomAnchor.constraint(equalTo: safeAreaLayoutGuide.bottomAnchor)
        ])
    }
}
*/
